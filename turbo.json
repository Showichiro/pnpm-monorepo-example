{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "check": {},
    "dev": {
      "dependsOn": ["check"],
      "cache": false
    },
    "build": {
      "dependsOn": ["check"],
      "cache": true,
      "outputs": ["dist/**"]
    }
  }
}
